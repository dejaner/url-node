version: '3'

services:
  db:
    image: mysql:8.0
    volumes:
      - db_data:/var/lib/mysql
    restart: always
    environment:
      MYSQL_ROOT_PASSWORD: root
      MYSQL_DATABASE: myurl
      MYSQL_USER: myurl
      MYSQL_PASSWORD: myurl

volumes:
  db_data: {}
